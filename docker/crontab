PATH=/usr/local/bin:/usr/bin:/bin

# WeatherLink data logger — runs every 30 minutes
*/30 * * * * python3 /app/wl_logger.py >> /proc/1/fd/1 2>&1

# WeatherLink daily email report — runs at 8 AM (uncomment to enable)
# 0 8 * * * python3 /app/wl_report.py >> /proc/1/fd/1 2>&1
